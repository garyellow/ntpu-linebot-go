# LINE Bot Configuration
# Required environment variables for NTPU LineBot

# LINE Channel Access Token
# Get this from LINE Developer Console > Your Channel > Messaging API
LINE_CHANNEL_ACCESS_TOKEN=your_access_token_here

# LINE Channel Secret
# Get this from LINE Developer Console > Your Channel > Basic settings
LINE_CHANNEL_SECRET=your_channel_secret_here

# Server Configuration
PORT=10000                      # HTTP server port
LOG_LEVEL=info                  # Log level: debug, info, warn, error
SHUTDOWN_TIMEOUT=30s            # Graceful shutdown timeout (supports: s, m, h)

# SQLite Configuration
SQLITE_PATH=/data/cache.db      # Database file path (Windows: ./data/cache.db, Linux: /data/cache.db)
CACHE_TTL=168h                  # Cache time-to-live (supports: s, m, h, e.g., 168h = 7 days)

# Scraper Configuration
SCRAPER_WORKERS=5               # Number of concurrent scraper workers
SCRAPER_MIN_DELAY=100ms         # Minimum delay between requests (supports: ms, s)
SCRAPER_MAX_DELAY=500ms         # Maximum delay between requests (supports: ms, s)
SCRAPER_TIMEOUT=15s             # HTTP request timeout (supports: s, m)
SCRAPER_MAX_RETRIES=3           # Maximum number of retry attempts

# Warmup Configuration
WARMUP_TIMEOUT=5m               # Total timeout for warmup process (supports: s, m)
